OPENROUTER_API_KEY=your_key_here

# AES-256-GCM key for at-rest column encryption (required).
# Must be a 32-byte base64url-encoded value. Generate with:
#   python -c "import base64,os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
DB_ENCRYPTION_KEY=your_32_byte_base64url_key_here
